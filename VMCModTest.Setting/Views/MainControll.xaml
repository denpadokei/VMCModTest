<UserControl x:Class="VMCModTest.Setting.Views.MainControll"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:VMCModTest.Setting.ViewModels"
             xmlns:local="clr-namespace:VMCModTest.Setting.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:MainControllViewModel />
    </UserControl.DataContext>
    <Grid>
        <StackPanel>
            <DockPanel HorizontalAlignment="Left">
                <TextBox Text="{Binding FilePath}" Width="300"></TextBox>
                <Button Content="…" Command="{Binding OpenFileDialogCommand}" Width="40"></Button>
            </DockPanel>
        </StackPanel>
    </Grid>
</UserControl>
